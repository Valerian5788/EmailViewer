<Window x:Class="EmailViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Email Viewer" Height="600" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ToolBar Grid.Row="0">
            <Button Content="Open Email" Click="OpenFileButton_Click"/>
        </ToolBar>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Recent Folders" Margin="10,5"/>
                <ListBox x:Name="recentFoldersListBox" Grid.Row="1" Margin="10" 
                         SelectionChanged="RecentFolderListBox_SelectionChanged"/>

                <TextBlock Grid.Row="2" Text="Emails" Margin="10,5"/>
                <ListBox x:Name="fileListBox" Grid.Row="2" Margin="10"/>
            </Grid>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <RichTextBox x:Name="emailContentRichTextBox" Grid.Row="0" Margin="10" 
                             IsReadOnly="True" VerticalScrollBarVisibility="Auto">
                    <RichTextBox.Resources>
                        <Style TargetType="{x:Type Paragraph}">
                            <Setter Property="Margin" Value="0"/>
                        </Style>
                    </RichTextBox.Resources>
                </RichTextBox>

                <DockPanel Grid.Row="1" LastChildFill="False">
                    <TextBox x:Name="notesTextBox" DockPanel.Dock="Left" 
                             Width="300" Margin="10" TextWrapping="Wrap"/>
                    <Button Content="Save Note" DockPanel.Dock="Right" 
                            Click="SaveNoteButton_Click" Margin="10"/>
                </DockPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>